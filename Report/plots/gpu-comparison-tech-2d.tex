\begin{tikzpicture}
	\newcommand{\AllTechnologies}{AMD+NV DX,AMD+NV GL}
	\begin{axis}[
		width=\linewidth,
		xmode = log,
		log basis x=2,
		grid = major,
		grid style = {
			dashed,
			gray!30},
		xlabel = $n$-point sequence,
		ylabel = Combined performance relative OpenCL,
		yticklabel pos=right,
		x unit = n,
		ymin = 0,
		ymax = 2,
		legend style = {
			at = {(0.05,0.95)}, 
			anchor = north west},
        ]        
        \addplot table[x=n,y=NORM,col sep=comma]{plots/2d.csv};
        \foreach \tech in \AllTechnologies {
			\addplot table[x=n,y=\tech,col sep=comma]{plots/2d.csv};
		}
		\legend{OpenCL,DirectCompute,OpenGL}
	\end{axis}
\end{tikzpicture}